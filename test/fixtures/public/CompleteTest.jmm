import io.a.b;
import c;

class Fac extends Uni {
    int number;
    int anotherNumber;
    int[] numArray;
    boolean isHappy;
    public int ComputeFac(int num) {
        int num_aux ;
        String word;
        if (num < 1)
            num_aux= 1;
        else
            num_aux = num * (this.ComputeFac(num-1));
        return num_aux;
    }
    public static void main(String[] args){
        io.println(new Fac().ComputeFac(10));
    }
}