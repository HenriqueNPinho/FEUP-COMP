import a.b.io;
import c;
import Unii;
import Uni;
import Feup;

class Fac extends Unii {
    int number;
    int anotherNumber;
    int[] numArray;
    boolean isHappy;

    public int bar() {
        return 0;
    }
    public int ComputeFac(int num) {
        int num_aux ;
        Uni uni;
        if (num < 1) {
            Feup.bar().foo();
            this.bar();
        }
        else
            num_aux = num * num_aux;
        return num_aux;
    }
    public static void main(String[] args){
        io.println(new Fac().ComputeFac(10));
    }
}